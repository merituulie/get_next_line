- buffer size (something small)
- norminette
- test runs
- read a line from standard input
- read lines from a file descriptor
- grep string.h, stdio.h, etc
1. read x bytes
2. stash to linked list
3. check if we reached the end of line
4. if not, continue reading
5. when a line break is found, set it to a variable
6. clean up stash up until line break
